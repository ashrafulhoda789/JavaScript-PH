const numbers = [1, 2, 3, 4, 5];

const evenNumbers = numbers.filter(num => num % 2 === 0);

console.log(evenNumbers);

const friends = ['Jamshed', 'Mumir', 'Alo', 'Tomato', 'Jammir'];
const JFriend = friends.filter(friend => friend[0] === 'J');
console.log(JFriend);